apiVersion: v1
kind: Service
metadata:
  name: {{ include "i18n-message.fullname" . }}
  labels:
    {{- include "i18n-message.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "i18n-message.selectorLabels" . | nindent 4 }}
